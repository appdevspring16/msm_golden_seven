<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Index</title>
  </head>
  <body>


    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/movies">MovieDB</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li class="active"><a href="/directors">Directors <span class="sr-only">(current)</span></a></li>
            <li class="active"><a href="/movies">Movies <span class="sr-only">(current)</span></a></li>
            <li class="active"><a href="/actors">Actors <span class="sr-only">(current)</span></a></li>
          </ul>

        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.6/paper/bootstrap.min.css" media="screen" title="no title" charset="utf-8">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.2/css/font-awesome.min.css" media="screen" title="no title" charset="utf-8">




    <div class="page-header">
      <h1>Actors</h1>
    </div>

    <p><a href="/actors/new_form" class="btn btn-primary">Add a New Actor</a></p>

    <table class="table table-hover">
      <thead>
        <tr>
          <th>Image</th>
          <th>Dob</th>
          <th>Name</th>
          <th>Bio</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @actors.each do |actor| %>
          <tr>
            <td><img src="<%= actor.image_url %>" width="80"></td>
            <td><%= actor.dob %></td>
            <td><%= actor.name %></td>
            <td><%= truncate(actor.bio) %></td>
            <td>
              <a href="/actors/<%= actor.id %>" class="btn btn-primary">
                <i class="fa fa-search-plus"></i>
              </a>
            </td>
            <td>
              <a href="/actors/<%= actor.id %>/edit_form" class="btn btn-warning">
                <i class="fa fa-edit"></i>
              </a>
            </td>
            <td>
              <a href="/delete_actor/<%= actor.id %>" class="btn btn-danger">
                <i class="fa fa-trash-o"></i>
              </a>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>


  </body>
</html>
